[package]
name = "host-tests"
version = "0.1.0"
edition = "2021"
description = "Host-side unit tests for agent pure-logic modules (lz4_block, ghostmem) — no NDK needed"

[dependencies]
libc = "0.2"
lz4_flex = "0.11"

[workspace]
